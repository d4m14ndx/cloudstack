import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "Kubernetes" };

export default function Page() {
  return <PageScaffold title="Kubernetes" description="CloudStack-managed Kubernetes clusters." />;
}
