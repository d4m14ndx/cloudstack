import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "Cluster" };

export default function Page() {
  return <PageScaffold title="Cluster" description="Kubernetes cluster detail." />;
}
