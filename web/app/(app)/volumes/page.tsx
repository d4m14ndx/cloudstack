import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "Volumes" };

export default function Page() {
  return <PageScaffold title="Volumes" description="Block storage volumes across your zones." />;
}
