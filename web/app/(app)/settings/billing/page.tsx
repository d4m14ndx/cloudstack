import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "Billing settings" };

export default function Page() {
  return <PageScaffold title="Billing" description="Invoices, usage exports, and payment settings." />;
}
