import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "Settings" };

export default function Page() {
  return <PageScaffold title="Settings" description="Profile, security, API tokens, integrations." />;
}
