import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "Integration settings" };

export default function Page() {
  return <PageScaffold title="Integrations" description="Identity, monitoring, and automation integrations." />;
}
