import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "API token settings" };

export default function Page() {
  return <PageScaffold title="API tokens" description="CloudStack API keys and scoped automation tokens." />;
}
