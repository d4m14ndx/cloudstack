import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "Security settings" };

export default function Page() {
  return <PageScaffold title="Security" description="Password, MFA, and session controls." />;
}
