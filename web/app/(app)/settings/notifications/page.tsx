import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "Notification settings" };

export default function Page() {
  return <PageScaffold title="Notifications" description="Email, event, and operational alert preferences." />;
}
