import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "Profile settings" };

export default function Page() {
  return <PageScaffold title="Profile" description="Personal details and console preferences." />;
}
