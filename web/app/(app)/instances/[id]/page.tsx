import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "Instance" };

export default function Page() {
  return <PageScaffold title="Instance" description="Detail view for a single instance." />;
}
