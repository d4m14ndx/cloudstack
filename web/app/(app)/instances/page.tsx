import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "Instances" };

export default function Page() {
  return <PageScaffold title="Instances" description="Virtual machines across all zones in your scope." />;
}
