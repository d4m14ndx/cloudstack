import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "Security groups" };

export default function Page() {
  return <PageScaffold title="Security groups" description="Firewall rule sets for instances." />;
}
