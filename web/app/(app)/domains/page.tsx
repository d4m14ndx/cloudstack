import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "Domains" };

export default function Page() {
  return <PageScaffold title="Domains" description="CloudStack tenancy hierarchy." />;
}
