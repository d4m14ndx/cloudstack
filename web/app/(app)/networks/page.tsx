import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "Networks" };

export default function Page() {
  return <PageScaffold title="Networks" description="VPCs, isolated networks, public IPs, and load balancers." />;
}
