import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "Network" };

export default function Page() {
  return <PageScaffold title="Network" description="Detail view for a single network." />;
}
