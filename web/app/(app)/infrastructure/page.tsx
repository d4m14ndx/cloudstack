import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "Infrastructure" };

export default function Page() {
  return <PageScaffold title="Infrastructure" description="Zones, pods, clusters, and hosts." />;
}
