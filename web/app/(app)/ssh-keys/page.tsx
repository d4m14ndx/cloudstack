import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "SSH keys" };

export default function Page() {
  return <PageScaffold title="SSH keys" description="SSH key pairs for instance access." />;
}
