import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "Billing & usage" };

export default function Page() {
  return <PageScaffold title="Billing & usage" description="Spend, forecasts, invoices, and budgets." />;
}
