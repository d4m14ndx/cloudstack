import { PageScaffold } from "@/components/page-header";

export const metadata = { title: "Events" };

export default function Page() {
  return <PageScaffold title="Events" description="Audit log of API and lifecycle events." />;
}
